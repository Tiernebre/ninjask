{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0",
    "husky": "^7.0.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "compile": "lerna run compile",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "format": "lerna run format",
    "prepare": "husky install"
  },
  "workspaces": [
    "packages/*"
  ]
}
